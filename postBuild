#!/bin/bash

# create lib folder
mkdir -p ~/lib && cd ~/lib

#
# Tikal
#
cd ~/lib
wget https://okapiframework.org/binaries/main/1.47.0/okapi-apps_gtk2-linux-x86_64_1.47.0.zip
unzip okapi-apps_gtk2-linux-x86_64_1.47.0.zip -d ./okapi
rm okapi-apps_gtk2-linux-x86_64_1.47.0.zip
cd ./okapi
chmod +x tikal.sh

#
# saxon-lint
#
cd ~/lib
git clone https://github.com/sputnick-dev/saxon-lint.git
cd saxon-lint*
chmod +x saxon-lint.pl

# add saxon-lint.pl and okapi to PATH variable
echo "PATH=\$PATH:~/lib/saxon-lint:~/lib/okapi" >> ~/.bashrc

# make aliases
echo "alias saxon-lint='saxon-lint.pl'" >> ~/.bashrc
echo "alias tikal='~/lib/okapi/tikal.sh'" >> ~/.bashrc

# validation
source ~/.bashrc

# go to home
cd ~/
